# Rushes / bloopers / ideas

Optional archive for visually interesting rejected generations, alternate ideas, and images the user explicitly asks to keep.

Nothing in this folder is canonical unless promoted into `approved/`.

Use descriptive names rather than keeping every failed generation by default; this folder is intentional curation, not an automatic dump.
